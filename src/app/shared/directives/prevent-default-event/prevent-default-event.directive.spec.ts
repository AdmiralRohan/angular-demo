import { PreventDefaultEventDirective } from "./prevent-default-event.directive";

describe("PreventDefaultEventDirective", () => {
	it("should create an instance", () => {
		const directive = new PreventDefaultEventDirective();
		expect(directive).toBeTruthy();
	});
});
